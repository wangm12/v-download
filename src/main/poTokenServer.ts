// TODO: Integrate bgutil-ytdlp-pot-provider for PO token support
// This will enable authenticated YouTube downloads when configured.

export function startPoTokenServer(): void {
  console.log('PO Token server not yet configured')
}

export function stopPoTokenServer(): void {
  // No-op for placeholder
}
